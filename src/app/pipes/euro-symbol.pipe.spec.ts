import { EuroSymbolPipe } from './euro-symbol.pipe';

describe('EuroSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new EuroSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
